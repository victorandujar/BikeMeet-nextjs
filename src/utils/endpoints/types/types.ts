export interface UserEndpointsStructure {
  users: string;
  login: string;
  register: string;
  verifyEmail: string;
  getUserIsVerified: string;
  findUserEmail: string;
  signup: string;
  restorePassword: string;
  recoveryEmailPassword: string;
  getUser: string;
}

export interface RideEndpointsStructure {
  dashboard: string;
}
